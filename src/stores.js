import { writable } from 'svelte-local-storage-store';

export const stored_visited_list_spain = writable('stored_visited_list_spain', {
})

export const stored_visited_list_france = writable('stored_visited_list_france', {
})

export const stored_visited_list_germany = writable('stored_visited_list_germany', {
})

export const stored_visited_list_italy = writable('stored_visited_list_italy', {
})

export const stored_visited_list_uk = writable('stored_visited_list_uk', {
})

export const stored_visited_list_us = writable('stored_visited_list_us', {
})

export const stored_visited_list_europe = writable('stored_visited_list_europe', {
})

export const stored_visited_list_japan = writable('stored_visited_list_japan', {
})